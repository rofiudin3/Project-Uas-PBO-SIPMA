# =============================================
#  SIPMA - Spring Boot Backend Configuration
# =============================================

# Server
server.port=${PORT:8080}

# Supabase PostgreSQL Database
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${DB_USERNAME:postgres.tsurthxhlqoglziodifp}
spring.datasource.password=${DB_PASSWORD:ne4*$BWKBrg3.r3}

# H2 Console (dinonaktifkan karena menggunakan PostgreSQL)
spring.h2.console.enabled=false
spring.h2.console.path=/h2-console 

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# CORS - izinkan akses dari frontend React
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}

# Mail (SMTP Configuration)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sipma.ifuinsgd@gmail.com
spring.mail.password=aahf oebs pdrn pakn
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000


# Nonaktifkan Spring Security default form-based login
spring.security.user.name=admin
spring.security.user.password=admin123

# ── Konfigurasi AI Server (Python FastAPI) ──
# URL endpoint deteksi atribut di Python server
# Jalankan AI server lebih dulu dengan: ai/run-ai.bat
ai.detection.url=http://localhost:8000/predict

# Fonnte Token
fonnte.token=${FONNTE_TOKEN:Rotated_Fonnte_Token_SIPMA_2026_Secure!}

